namespace Task8
{
    internal class Accauntant
    {
        public bool AskForBonus(Post worker, int hours)
        {
            if (hours >= (int)worker)
            {
                return true;
            }
            return false;
        }
    }
}
